# R2-Dreamer Patches

This directory documents patches applied to the external `NM512/r2dreamer` checkout.

Patch scripts live in `scripts/r2dreamer/` and modify only the external checkout passed via `--r2-repo`. The external repository is not committed here.
