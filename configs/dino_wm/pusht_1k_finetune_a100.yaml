extends: base.yaml
run_name: pusht_1k_finetune_a100_seed0
dataset:
  env: pusht_noise
  max_train_trajectories: 300
  max_val_trajectories: 60
finetuning:
  enabled: true
  init_from: ${oc.env:DINO_PUSHT_SOURCE_CKPT,null}
  epochs: 20
training:
  seed: 0
  max_wall_minutes: 100
planning:
  n_evals: 50
  max_wall_minutes: 60
metadata:
  tags:
    - pusht
    - finetune
